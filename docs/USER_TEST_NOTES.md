# 用户测试反馈待办

## 已处理，待安装包验证

- 生成结果页：将「返回」和「下一步」固定放在「自然英语已生成」同一行，不再放在页面底部。
- 发音生成：并发生成数从 3 句提高到 5 句，长段落的发音等待时间应明显缩短。
- 底部导航：四个图标支持点击切换页面，同时保留左右滑动和跟手动画。
- 使用教程：首次打开先介绍使用场景与核心学习方法，再展示四步操作；账号设置中可随时重新查看。

## 第二位测试用户：已处理，待安装包验证

- 使用教程：保留「上一步 / 下一步」按钮，同时支持左滑下一页、右滑上一页，卡片会跟随手指移动。
- 中文识别速度：应用启动和开始录音时提前唤醒 Render 后端，尽量把免费实例冷启动隐藏在用户阅读教程和录音期间。
- 录音上传：从 44.1 kHz 双声道 128 kbps 调整为适合语音识别的 16 kHz 单声道 64 kbps，减少移动网络上传数据量。
- 性能基线：后端已唤醒时，健康接口约 1.13 秒；标准中文测试录音完整识别约 4.52 秒。

## 待逐项处理

- 中文识别仍需在第二位测试用户的真实手机和网络下复测；长段落的发音生成已优先提速。
